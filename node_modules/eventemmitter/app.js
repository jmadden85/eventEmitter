 
/*
 * Defining the Package
 */

var Module = require("meanio").Module;

var Eventemmitter = new Module("eventemmitter");

/*
 * All MEAN packages require registration
 * Dependency injection is used to define required modules
 */

Eventemmitter.register(function(app, auth, database) {

    //We enable routing. By default the Package Object is passed to the routes
    Eventemmitter.routes(app, auth, database);

    //We are adding a link to the main menu for all authenticated users
    Eventemmitter.menus.add({
      title: "eventemmitter example page",
      link: "eventemmitter example page",
      roles: ["authenticated"],
      menu: "main"
    })
});