'use strict';

angular.module('mean').controller('EventemmitterController', ['$scope', 'Global',
	function($scope, Global, Eventemmitter) {
		$scope.global = Global;
		$scope.eventemmitter = {name:'eventemmitter'};

	}
]);