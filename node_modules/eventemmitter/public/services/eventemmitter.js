'use strict';

//Events service used for articles REST endpoint
angular.module('mean.events').factory('Events', ['$resource', function($resource) {
    return $resource('events', {
    }, {
        update: {
            method: 'PUT'
        }
    });
}]);